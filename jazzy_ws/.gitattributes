# SCM syntax highlighting & preventing 3-way merges
pixi.lock merge=binary linguist-language=YAML linguist-generated=true -diff

# USD crate files (binary despite the .usd extension) cannot be text-merged.
# .usda stays text, so it diffs and merges normally.
*.usd binary
*.usdc binary
